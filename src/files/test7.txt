class Test7
int a, b, c;
string texto;
{
    write("Digite o valor de a:");
    read (a);
    write("Digite o valor de c:");
    read (c);

    if(b < a){
        texto = "o valor de " + b + "é menor que " + a;
    } else {
        texto = "o valor de  " + b + "é maior que " + a;
    };
    do {
        c = 2;
    } while (a != b);
    write(texto);
}

